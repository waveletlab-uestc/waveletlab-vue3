# ICCWAMTIP 2026 网站维护指南

这份指南面向第一次接触本项目的维护者。按“快速上手”操作，通常可以在 10 分钟内启动网站；按“常见任务”操作，可以完成日常内容更新、验证、提交和发布。

> 项目地址：<https://github.com/waveletlab-uestc/waveletlab-vue3>
>
> 正式域名：<https://waveletlab.cn/>
>
> 默认分支：`main`

## 目录

- [1. 10 分钟快速上手](#1-10-分钟快速上手)
- [2. 先理解项目](#2-先理解项目)
- [3. 常见维护任务](#3-常见维护任务)
- [4. 中英文与会议数据规则](#4-中英文与会议数据规则)
- [5. 文件上传与下载链接](#5-文件上传与下载链接)
- [6. GitHub 协作流程](#6-github-协作流程)
- [7. CI/CD 与 GitHub Pages](#7-cicd-与-github-pages)
- [8. 故障排查与回滚](#8-故障排查与回滚)
- [9. AI 辅助维护](#9-ai-辅助维护)
- [10. 发布检查清单](#10-发布检查清单)

## 1. 10 分钟快速上手

### 1.1 准备环境

需要安装：

- Git；
- Node.js 22（CI 使用的版本）；
- npm（随 Node.js 安装）；
- 任意代码编辑器，推荐 VS Code。

检查环境：

```bash
git --version
node --version
npm --version
```

### 1.2 获取代码

只有查看权限或准备提交 PR 时，先 Fork 主仓库，再克隆自己的 Fork：

```bash
git clone https://github.com/你的用户名/waveletlab-vue3.git
cd waveletlab-vue3
git remote add upstream https://github.com/waveletlab-uestc/waveletlab-vue3.git
git remote -v
```

如果你是仓库管理员，也建议从最新 `main` 创建功能分支，不要直接修改 `main`。

### 1.3 安装并启动

仓库已经提交 `package-lock.json`，首次安装和 CI 对齐时使用：

```bash
npm ci
npm run dev
```

终端会显示本地地址，通常是 `http://localhost:5173/`。打开后至少检查首页、一个内页和中英文切换。

常用命令：

| 命令 | 用途 |
| --- | --- |
| `npm ci` | 按锁文件安装依赖，适合全新环境和 CI |
| `npm run dev` | 启动本地开发服务器 |
| `npm run build` | 构建生产版本，并生成 `.html` 兼容入口 |
| `npm run preview` | 本地预览刚生成的 `dist/` |
| `git diff --check` | 检查多余空格等格式问题 |

### 1.4 完成第一次修改

1. 从最新 `main` 创建任务分支；
2. 用下方“改哪里”表找到文件；
3. 修改后在本地检查页面；
4. 运行验证命令；
5. 提交 Pull Request；
6. PR 构建通过并审核合并后，GitHub Pages 自动发布。

```bash
git switch -c update-conference-content
# 修改文件
npm run build
git diff --check
git status --short
git diff
```

### 1.5 改哪里

| 需求 | 首选文件 |
| --- | --- |
| 修改中英文文字、日期、新闻、人员、链接 | `src/i18n/index.js` |
| 修改页面结构 | `src/pages/*.vue` |
| 修改页头、页脚、侧栏等公共区域 | `src/components/*.vue` |
| 修改颜色、间距、移动端样式 | `src/assets/styles.css` |
| 新增或修改路由 | `src/router/index.js` |
| 增加 `.html` 兼容入口 | `scripts/copy-routes.mjs` |
| 增加小图片或站点必需文件 | `public/` |
| 修改构建或部署 | `package.json`、`vite.config.js`、`.github/workflows/deploy.yml` |

日常内容维护通常只需要修改 `src/i18n/index.js`。

## 2. 先理解项目

### 2.1 技术与运行方式

本项目是一个 Vue 3 + Vite 中英文会议网站：

- Vue 3 负责页面和组件；
- Vue Router 负责页面路由；
- Element Plus 提供部分界面组件；
- GitHub Actions 负责 CI 构建检查和 CD 部署；
- GitHub Pages 托管构建后的站点；
- jsDelivr / GitHub Raw 提供公开下载资源。

### 2.2 目录结构

```text
.
├── .github/workflows/deploy.yml  # CI/CD 工作流
├── public/                       # 构建时原样复制的站点文件
│   ├── CNAME                     # 正式域名
│   ├── doc/                      # 少量站内文档
│   └── images/                   # Logo、嘉宾头像等
├── scripts/copy-routes.mjs       # 生成 404.html 和各页面兼容入口
├── src/
│   ├── assets/styles.css         # 全站样式
│   ├── components/               # 公共组件
│   ├── i18n/index.js             # 中英文内容和主要会议数据
│   ├── pages/                    # 页面组件
│   ├── router/index.js           # 路由
│   ├── App.vue
│   └── main.js
├── AGENTS.md                     # AI Agent 仓库操作说明
├── index.html
├── package.json
├── package-lock.json
└── vite.config.js
```

不要手工修改或提交 `node_modules/`、`dist/`。它们分别是本地依赖和构建产物，已由 `.gitignore` 排除。

### 2.3 页面与地址

| 页面文件 | 正式地址 |
| --- | --- |
| `HomePage.vue` | `/` |
| `InformationPage.vue` | `/information.html` |
| `GuidancePage.vue` | `/guidance.html` |
| `TimelinePage.vue` | `/timeline.html` |
| `TopicsPage.vue` | `/topics.html` |
| `SpeakersPage.vue` | `/speakers.html` |
| `CommitteesPage.vue` | `/committees.html` |
| `ContactPage.vue` | `/contact.html` |
| `NewsPage.vue` | `/news.html` |

项目使用 History 路由。`npm run build` 除了执行 Vite 构建，还会运行 `scripts/copy-routes.mjs`，把 `dist/index.html` 复制成上述 `.html` 入口和 `404.html`，保证 GitHub Pages 可以直接打开旧式地址。

## 3. 常见维护任务

### 3.1 修改普通文字

1. 在 `src/i18n/index.js` 搜索页面上现有的一小段文字；
2. 确认它属于 `messages.en` 还是 `messages.zh`；
3. 同步修改另一种语言中结构相同的字段；
4. 本地切换两种语言复核；
5. 搜索旧文字，确认没有漏改。

```bash
rg -n "旧文字的一部分" src
```

### 3.2 修改会议日期或截止日期

日期可能同时出现在会议概要、重要日期、时间线、作者指南和新闻中。至少检查：

- `conference.date`；
- `importantDates`；
- `timelineRows`；
- `latestNews`；
- `guidance` 下的相关内容；
- 下载版征稿通知是否也需要更新。

用新旧日期的中英文写法做全文搜索：

```bash
rg -n "August 10|Aug. 10|8月10日|8 月 10 日" src public
```

历史新闻记录的是“当时发布了什么”，不能为了统一当前日期而改写旧新闻。例如 7 月 31 日宣布截止日期从 8 月 10 日延期至 8 月 31 日，7 月 7 日旧新闻中的 8 月 10 日应保留。

### 3.3 新增新闻

中英文 `latestNews` 都要新增对应条目，并按时间倒序排列，最新消息放在数组最前面：

```js
// messages.en.latestNews
{
  date: '2026-07-31',
  text: 'The paper submission deadline has been extended to August 31st, 2026.',
},

// messages.zh.latestNews
{
  date: '2026-07-31',
  text: '论文投稿截止日期已延期至2026年8月31日。',
},
```

日期字段使用 `YYYY-MM-DD`，不要删除历史新闻。

### 3.4 修改共享联系方式或投稿链接

邮箱、电话、CMT 投稿地址和投稿说明位于文件顶部的 `sharedConferenceLinks`。这些值会被中英文共同使用，只修改一次即可。出版编号位于 `catalogNumbers`，嘉宾图片路径位于 `speakerImages`。

修改后仍要全文搜索旧值，防止页面正文中存在单独写死的副本：

```bash
rg -n "旧邮箱|旧电话号码|旧链接" src public
```

### 3.5 新增嘉宾或委员会成员

- 嘉宾头像放入 `public/images/`，文件名使用小写英文和连字符；
- 在 `speakerImages` 添加图片映射；
- 在中英文 `speakers` 中添加结构一致的内容；
- 委员会的共享姓名列表位于 `committeeMembers`；
- 本地检查图片比例、缺图状态、长姓名换行和移动端布局。

### 3.6 新增页面

需要同时完成：

1. 在 `src/pages/` 新建页面组件；
2. 在 `src/router/index.js` 导入组件并注册路由；
3. 在中英文 `routes`、`primaryNav` 或 `sideNav` 中添加所需入口；
4. 如果地址以 `.html` 结尾，在 `scripts/copy-routes.mjs` 的 `routeEntries` 中加入相同文件名；
5. 运行构建并确认 `dist/对应页面.html` 存在；
6. 直接访问新地址，检查刷新后仍可打开。

多个页面共用的结构应放入 `src/components/`，不要在不同页面复制相同模板。

### 3.7 修改样式

全站样式集中在 `src/assets/styles.css`。优先复用现有类名、颜色变量和响应式断点。修改后至少检查：

- 桌面端和手机宽度；
- 中英文长短差异；
- 页头导航展开/收起；
- 表格、按钮、链接和焦点状态；
- 首页以及受影响的内页。

## 4. 中英文与会议数据规则

中英文内容集中在 `src/i18n/index.js`：

- `messages.en` 是英文；
- `messages.zh` 是中文；
- 两种语言的对象结构和键名必须一致；
- 日期、链接、人员和新闻修改后都要检查两种语言；
- `t()` 用于文本路径，`tm()` 用于数组和对象；
- 用户语言保存在浏览器的 `iccwamtip-locale` 中，默认语言是英文。

以下数据刻意放在 `messages` 外供两种语言共享：

| 变量 | 内容 |
| --- | --- |
| `assetCdnBase` | 静态资源 CDN 基础地址 |
| `latexTemplateUrl` | LaTeX 模板下载地址 |
| `bookletUrl` | 大文件下载地址 |
| `sharedConferenceLinks` | 邮箱、电话、投稿链接 |
| `catalogNumbers` | 出版编号 |
| `speakerImages` | 嘉宾图片路径 |
| `committeeMembers` | 委员会人员名单 |

除非中英文确实需要不同内容，否则不要把共享数据复制回两个语言对象。

## 5. 文件上传与下载链接

### 5.1 文件放在哪里

网站仓库的 `public/` 只保存与页面运行紧密相关的小文件，例如：

- `CNAME`；
- Logo 和嘉宾头像；
- 当前站点必须直接访问的小型文档。

公开下载文件、大型 PDF、历年材料和经常更新的模板放入独立资源仓库：

```text
https://github.com/waveletlab-uestc/oos
```

建议按年份和类型整理，例如：

```text
2026/docs/
2026/images/
2026/templates/
```

新文件名应包含年份或更新日期，不要覆盖旧版本：

```text
CallforPaper-EN260831.pdf
PaperFormat-2026-EN-260831.dotx
```

### 5.2 小于 20 MB：jsDelivr

```text
https://cdn.jsdelivr.net/gh/组织名/仓库名@完整提交SHA/文件路径
```

示例：

```text
https://cdn.jsdelivr.net/gh/waveletlab-uestc/oos@完整提交SHA/2026/introduction2026.png
```

资源上传流程：

1. 把文件提交并推送到 `waveletlab-uestc/oos`；
2. 获取 `main` 的最新完整提交 SHA；
3. 生成并访问 jsDelivr 地址；
4. 确认 HTTP 200、文件大小和类型正确；
5. 替换网站源码中的旧链接；
6. 搜索旧文件名和旧 SHA，确认没有遗漏；
7. 运行 `npm run build`。

```bash
git ls-remote https://github.com/waveletlab-uestc/oos.git refs/heads/main
curl -I "https://cdn.jsdelivr.net/gh/waveletlab-uestc/oos@完整提交SHA/文件路径"
```

正式网站必须固定完整提交 SHA，不要使用 `@main`。固定版本可以避免 CDN 缓存导致多个文件新旧混用。

### 5.3 超过 20 MB：GitHub Raw

jsDelivr 对超过 20 MB 的单个 GitHub 文件会返回 403，改用固定提交的 Raw 地址：

```text
https://raw.githubusercontent.com/组织名/仓库名/完整提交SHA/文件路径
```

当前 `booklet2023.pdf` 使用这种方式。更大的长期下载文件应考虑对象存储，不要持续增大 Git 仓库。

## 6. GitHub 协作流程

### 6.1 开始任务

项目默认采用 Fork + Pull Request。第一次参与时按 1.2 节配置 `origin` 和 `upstream`。以后每次任务开始前：

```bash
git switch main
git fetch upstream
git merge --ff-only upstream/main
git push origin main
git switch -c update-submission-deadline
```

分支名应简短并说明任务，例如：

```text
update-submission-deadline
update-call-for-paper
fix-mobile-navigation
add-2026-speaker
```

如果工作区已有未提交修改，先确认修改来源，不要直接丢弃或覆盖。

### 6.2 修改、验证和提交

```bash
npm ci
npm run build
git diff --check
git status
git diff
git add 本次任务相关文件
git commit -m "更新投稿截止日期"
git push -u origin update-submission-deadline
```

尽量只暂存本次任务相关文件，不要把无关改动、`node_modules/`、`dist/` 或大型资源提交进来。

### 6.3 创建 Pull Request

PR 应指向：

```text
base repository: waveletlab-uestc/waveletlab-vue3
base branch: main
head repository: 你的用户名/waveletlab-vue3
compare branch: 本次任务分支
```

推荐描述模板：

```markdown
## 修改内容

- 将投稿截止日期更新为 2026 年 8 月 31 日
- 同步更新中英文重要日期和时间线
- 新增延期新闻

## 验证

- [x] npm run build
- [x] git diff --check
- [x] 中英文内容已同步
- [x] 已检查旧日期残留
- [x] 已检查桌面端和移动端
```

PR 提交后等待 GitHub Actions 的构建检查。需要调整时在原分支继续提交并推送，PR 会自动更新。

### 6.4 审核、合并和清理

审核重点：任务范围、中英文一致性、历史新闻、外链、移动端以及 CI。通常使用 **Squash and merge**，让一次维护任务在 `main` 中只留下一个清晰提交。

合并后删除 GitHub 上的功能分支，再同步本地：

```bash
git switch main
git fetch upstream
git merge --ff-only upstream/main
git push origin main
git branch -d update-submission-deadline
```

下一次任务从最新 `main` 新建分支，不复用已合并分支。

## 7. CI/CD 与 GitHub Pages

工作流文件是 `.github/workflows/deploy.yml`，部署源必须在 GitHub 仓库的 **Settings → Pages → Build and deployment → Source** 中设置为 **GitHub Actions**。

### 7.1 何时触发

| 事件 | CI 构建检查 | 上传 Pages 产物 | 部署正式站 |
| --- | --- | --- | --- |
| PR 指向 `main` | 是 | 否 | 否 |
| 推送/合并到 `main` | 是 | 是 | 是 |
| 手动运行 `workflow_dispatch` | 是 | 是 | 是 |

工作流触发分支必须与默认分支 `main` 一致。如果误写成 `master`，合并到 `main` 后不会自动发布。

### 7.2 CI 做什么

```text
检出代码
→ 安装 Node.js 22
→ npm ci
→ npm run build
→ 检查 dist/index.html、dist/CNAME 和生产资源引用
```

PR 到这里结束，因此 PR 只能证明代码可以生成正确的生产产物，不会改动正式网站。

### 7.3 CD 做什么

```text
main 构建通过
→ 上传 dist 为 GitHub Pages artifact
→ deploy job 使用 github-pages 环境
→ actions/deploy-pages 发布
→ 输出正式页面地址
```

工作流使用 `pages: write` 和 `id-token: write` 权限，并用 `pages` 并发组避免部署互相覆盖。不要改成直接提交 `dist/`，也不要把 Pages Source 切换成从分支目录部署。

### 7.4 发布后确认

1. 打开仓库 **Actions**，确认 `Build and Deploy` 为绿色；
2. 打开部署记录中的页面地址；
3. 访问正式域名并强制刷新；
4. 检查本次修改、中英文和移动端；
5. 直接打开受影响的 `.html` 地址，确认刷新正常；
6. 如果改了下载资源，点击实际下载一次。

## 8. 故障排查与回滚

### 8.1 本地无法启动

- 确认 Node.js 为 22；
- 删除依赖重装前先确认目标确实是项目内的 `node_modules/`；
- 优先运行 `npm ci`，不要随意更新依赖版本；
- 检查终端里 Vite 输出的实际端口。

### 8.2 `npm ci` 失败

如果提示 `package.json` 与 `package-lock.json` 不一致，说明依赖声明和锁文件没有一起提交。确实需要改依赖时运行 `npm install` 更新锁文件，然后同时提交两者；纯内容修改不应改变任何依赖文件。

### 8.3 构建失败

先在本地运行 `npm run build`，根据第一条错误定位文件。常见原因包括：

- JavaScript 对象少逗号、引号或括号；
- Vue 模板标签未闭合；
- 新页面已加路由但文件路径或大小写错误；
- `scripts/copy-routes.mjs` 在构建入口生成阶段报错。

### 8.4 PR 通过但正式站没更新

PR 只运行 CI，不部署。确认 PR 已合并到 `main`，然后检查：

- `main` 对应的 Actions 是否成功；
- Pages Source 是否为 GitHub Actions；
- 是否查看了正确域名；
- 浏览器或 CDN 是否仍在缓存旧内容。

### 8.5 GitHub Pages 404 或自定义域名失效

- 确认 `public/CNAME` 存在且内容为 `waveletlab.cn`；
- 确认构建记录验证了 `dist/CNAME`；
- 确认新 `.html` 路由同时加入 `src/router/index.js` 和 `scripts/copy-routes.mjs`；
- 确认 DNS 和 GitHub Pages 的 Custom domain 设置没有被改动。

### 8.6 下载链接 403 或仍是旧文件

- 单文件超过 20 MB 时不要使用 jsDelivr；
- 确认 URL 中的完整 SHA 对应包含该文件的提交；
- 检查路径大小写和 URL 编码；
- 新版本使用新文件名和新 SHA，不要依赖覆盖后的 CDN 刷新。

### 8.7 回滚错误发布

不要直接在 GitHub Pages 上改构建产物。对 `main` 上的错误提交创建一个反向提交并推送，Actions 会重新部署：

```bash
git switch main
git pull --ff-only
git revert 错误提交SHA
git push
```

如果错误改动来自合并 PR，仓库管理员可以在 GitHub 的 PR 页面使用 **Revert** 创建回滚 PR。回滚前先确认目标提交和影响范围；不要使用强制推送改写共享分支历史。

## 9. AI 辅助维护

仓库根目录的 `AGENTS.md` 是给 AI Agent 的项目说明。它定义了修改位置、中英文规则、资源规则、验证命令和 Git 安全边界。使用 AI 时仍应提供本次任务的具体事实，不要只说“更新一下网站”。

推荐任务描述：

```text
把投稿截止日期延期到 2026 年 8 月 31 日。
同步修改中英文的重要日期、时间线和最新新闻，历史新闻保持原样。
先搜索所有旧日期和影响位置；修改后运行 npm run build、git diff --check，
并报告仍然保留旧日期的位置及保留原因。不要提交或推送。
```

资源更新示例：

```text
文件已经上传到 waveletlab-uestc/oos 的 2026 目录。
请获取最新完整提交 SHA，验证 jsDelivr 地址后替换旧链接；
超过 20 MB 使用固定提交的 GitHub Raw。搜索旧文件名和旧 SHA，
运行构建并报告结果，不要修改其他内容。
```

人工复核始终包括：

- 查看完整 `git diff`，确认没有无关修改；
- 核对 AI 没有改写历史新闻；
- 检查中英文、链接、文件版本和页面显示；
- 确认 `git status` 中没有密钥、本地配置或大型文件；
- 除非明确授权，不让 AI 合并 PR、强制推送或删除远程分支。

## 10. 发布检查清单

### 内容与页面

- [ ] 中英文内容和数据结构同步
- [ ] 当前日期在首页、信息页、时间线和作者指南中一致
- [ ] 历史新闻没有被错误改写，新闻保持倒序
- [ ] 旧文字、旧日期、旧文件名和旧链接已全文搜索
- [ ] 桌面端、移动端和两种语言显示正常

### 文件与链接

- [ ] 小型站点文件放在 `public/`，大型下载文件没有放入网站仓库
- [ ] 新资源已上传到 `waveletlab-uestc/oos`
- [ ] jsDelivr 或 GitHub Raw 地址返回 200
- [ ] 远程地址固定完整提交 SHA
- [ ] 实际点击下载后，文件类型和版本正确

### 代码与协作

- [ ] 修改位于独立功能分支且只包含本次任务
- [ ] `npm run build` 成功
- [ ] `git diff --check` 通过
- [ ] `git status` 和完整 `git diff` 已人工检查
- [ ] Pull Request 的 GitHub Actions 检查通过

### 发布

- [ ] PR 已审核并合并到 `main`
- [ ] `main` 的 GitHub Pages 部署成功
- [ ] 正式域名已复核，直接访问 `.html` 地址正常
- [ ] 必要时已准备回滚提交或回滚 PR
